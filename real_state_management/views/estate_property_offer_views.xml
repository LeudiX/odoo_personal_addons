<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View estate.property.offer View Tree -->
    <record id="view_estate_property_offer_tree" model="ir.ui.view">
        <field name="name">view.estate.property.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree editable="bottom" decoration-success="status == 'accepted'"  decoration-danger="status == 'refused'">
                <field name="partner_id" widget="many2one_avatar_user"/>
                <field name="price"/>
                <field name="validity_days"/>
                <field name="date_deadline"/>
                <button name="action_accept_offer" type="object" title="Accept" icon="fa-check" invisible="status in ['accepted']"/>
                <button name="action_refuse_offer" type="object" title="Refuse" icon="fa-times" invisible="status in ['refused']"/>
                <field name="status" invisible="not status"/>
            </tree>
        </field>
    </record>

    <!-- View estate.property.offer form -->
    <record id="view_estate_property_offer_form" model="ir.ui.view">
        <field name="name">view.estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity_days"/>
                        <field name="date_deadline"/>
                        <field name="status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
