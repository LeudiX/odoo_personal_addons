<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View estate.property View Tree -->
    <record id="view_estate_property_tree" model="ir.ui.view">
        <field name="name">view.estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                 <field name="best_price"/>
                <field name="best_bidder_id" widget="many2one_avatar_user"/>
                <field name="selling_price"/>
                <field name="availability_date"/>
            </tree>
        </field>
    </record>

    <!-- View estate.property form -->
    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">view.estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
                <header>
                     <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                        <label for="tag_ids"/>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    </div>
                    <group class="mt-4">
                        <group>
                            <field name="property_type_id"/>
                            <field name="postcode"/>
                            <field name="availability_date"/>
                        </group>   
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/> <!--Computed fields are readonly by default-->
                            <field name="best_bidder_id"/>
                            <field name="selling_price"/>
                        </group>   
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area" readonly="1"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <tree>
                                <field name="offer_ids" readonly="state in ('offer_accepted','sold','canceled')"/>
                            </tree>
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="salesperson_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                 <div class="oe_chatter">
                    <field name="message_follower_ids" />
                    <field name="activity_ids" />
                    <field name="message_ids" options="{'post_refresh': 'recipients'}" />
                </div>
            </form>
        </field>
    </record>

    <!-- View estate.property search -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">view.estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="facades"/>
                <field name="garage"/>
                <field name="garden"/>
                <filter name="Available" string="Available" domain="[('state', 'in', ('new','offer_received'))]" context="{'group_by': 'availability_date'}" />
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                    <filter string="Postcode" name="postcode" domain="[]" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action estate.property -->
    <record id="action_estate_property" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_smiling_face">
                Create a property advertisement
            </p>
            <p>
                Create real state properties and follow the selling process
            </p>
        </field>
    </record>

</odoo>
